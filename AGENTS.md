## Python environment

Use the repository-local `.venv` for all Python commands. On Windows, invoke
`.\.venv\Scripts\python.exe` directly; for example:

```powershell
.\.venv\Scripts\python.exe -m pytest
```
